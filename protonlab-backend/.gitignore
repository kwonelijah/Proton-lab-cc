# Dependencies
node_modules/

# Environment variables — NEVER commit these
.env
.env.local
.env.production

# Vercel local files (project.json contains IDs but not secrets — safe to commit)
.vercel/cache

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
