{
  "name": "protonlab-backend",
  "version": "1.0.0",
  "description": "ProtonLab order processing backend — Stripe + Google Sheets + Claude agent",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.0",
    "googleapis": "^140.0.0",
    "stripe": "^14.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
